---
import DocsLayout from '../../../../layouts/DocsLayout.astro';
---
<DocsLayout title="安装" slug="usage/install" lang="zh" description="安装 Skill Wiki —— bun、Node 22+、prime CLI 与 MCP server。">
  <h1>安装</h1>
  <p style="color: var(--fg-muted);">
    Skill Wiki 以一组 npm 包发布。CLI 负责编译 / 发布 / 安装；MCP server 把编译好的 corpus 暴露给你的 agent。
  </p>

  <h2>环境要求</h2>
  <ul>
    <li><strong>Node 22+</strong>（<a href="https://nodejs.org" rel="noopener">nodejs.org</a>）</li>
    <li>推荐 <strong>Bun 1.3+</strong>（<a href="https://bun.sh" rel="noopener">bun.sh</a>）—— 编译阶段比 Node 快很多</li>
    <li>一个支持 MCP 的客户端（Claude Code、Cursor、Continue、Aider 等）</li>
  </ul>

  <h2>安装 CLI</h2>
<pre><code>{`# Bun (recommended)
bun add -g @prime-lang/cli

# Or npm
npm install -g @prime-lang/cli

# Verify
prime --version
# → prime 0.1.0`}</code></pre>

  <h2>从市集装一个 Skill</h2>
<pre><code>{`# The flagship pack — frontend-design
prime install @frontend-design

# A minimal example
prime install @recipes`}</code></pre>

  <p>
    默认安装到 <code>./.primes/</code>。需要改路径用 <code>--dir</code>。
  </p>

  <h2>启动 MCP server</h2>
<pre><code>{`# Stdio transport (for Claude Code / Cursor)
PRIME_DIR=./.primes/@recipes bunx @prime-lang/mcp-server-core

# HTTP transport (for browser-based clients)
PRIME_DIR=./.primes/@recipes \\
  bunx @prime-lang/mcp-server-core --http --port 4001`}</code></pre>

  <h2>从源码装</h2>
  <p>如果你要给 Skill Wiki 自己开发，或跑还没发布的版本：</p>

<pre><code>{`git clone https://github.com/skill-wiki/prime-system.git
cd prime-system
bun install
bun run build
bun run packages/cli/src/index.ts --version`}</code></pre>

  <h2>验证安装</h2>
<pre><code>{`# Compile and inspect the bundled hello-world example
cd examples/hello-world
prime compile

# List atoms
prime list
# → @hello-world/method-make-tea         method
# → @hello-world/term-celsius            term
# → @hello-world/fact-water-boils-at-100c fact
# → @hello-world/rule-altitude-affects-boiling rule`}</code></pre>

  <h2>下一步</h2>
  <ul>
    <li><a href="/zh/docs/usage/first-atom">写你的第一个原子</a></li>
    <li><a href="/zh/docs/usage/mcp-claude">接入 Claude Code</a></li>
    <li><a href="/zh/docs/usage/publish-install">发布与安装自己的 Skill</a></li>
  </ul>
</DocsLayout>
