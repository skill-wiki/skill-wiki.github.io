---
import DocsLayout from '../../../../layouts/DocsLayout.astro';
---
<DocsLayout title="安装" slug="usage/install" lang="zh" description="安装 Skill Wiki —— bun、Node 22+、prime CLI 与 MCP server。">
  <h1>安装</h1>
  <p style="color: var(--fg-muted);">
    Skill Wiki 是一组用 bun 跑的 TypeScript 包。CLI 负责编译 / 发布 / 安装；
    MCP server 把编译好的 corpus 暴露给你的 agent。
  </p>

  <h2>环境要求</h2>
  <ul>
    <li><strong>Node 22+</strong>（<a href="https://nodejs.org" rel="noopener">nodejs.org</a>）</li>
    <li><strong>Bun 1.3+</strong> 必装（<a href="https://bun.sh" rel="noopener">bun.sh</a>），CLI 与 MCP server 都依赖它</li>
    <li>一个支持 MCP 的客户端（Claude Code、Cursor、Continue、Aider 等）</li>
  </ul>

  <h2>安装 CLI</h2>
  <p>当前装法是从源码装 —— npm 发布排在 v0.1.1。</p>

<pre><code>{`# 克隆、装依赖、把 CLI link 到全局
git clone https://github.com/skill-wiki/prime-system.git
cd prime-system
bun install
bun link

# 新开一个 shell，按名字 link 二进制（一次性）
bun link @prime-lang/cli

# 验证
prime --version
# → prime 0.1.0`}</code></pre>

  <p style="color: var(--fg-muted); border-left: 3px solid var(--accent, #888); padding: 0.5rem 0.75rem; background: var(--bg-subtle, transparent);">
    <strong>即将到来：</strong>一旦 v0.1.1 把 <code>@prime-lang/cli</code> 发到 npm，
    就可以直接 <code>bun add -g @prime-lang/cli</code>。
  </p>

  <h2>从市集装一个 Prime</h2>
<pre><code>{`# 旗舰 Prime —— frontend-design（先用 clone，registry 在 v0.1.1）
git clone https://github.com/skill-wiki/prime-corpus-frontend.git

# 最小示例就在 prime-system 仓库里
cd prime-system/examples/hello-world`}</code></pre>

  <h2>启动 MCP server</h2>
  <p>server 入口就是 clone 下来的仓库里那个 bun 可执行的 TypeScript 文件。</p>

<pre><code>{`# 通用 1 工具 server（任意编译好的 corpus）
PRIME_DIR=/abs/path/to/compiled \\
  bun /abs/path/to/prime-system/packages/mcp-server-core/src/index.ts

# 前端 5 工具 server（frontend-design corpus）
PRIME_DIR=/abs/path/to/prime-corpus-frontend/compiled-v3-final \\
  bun /abs/path/to/prime-corpus-frontend/app/mcp-server-frontend/index.ts`}</code></pre>

  <p>接入 Claude Code / Cursor / Desktop 的细节见
  <a href="/zh/docs/usage/mcp-claude">MCP 接入页</a>。</p>

  <h2>验证安装</h2>
<pre><code>{`# 编译并看一眼自带的 hello-world 示例
cd prime-system/examples/hello-world
prime compile

# 列原子
prime list
# → @hello-world/method-make-tea         method
# → @hello-world/term-celsius            term
# → @hello-world/fact-water-boils-at-100c fact
# → @hello-world/rule-altitude-affects-boiling rule`}</code></pre>

  <h2>下一步</h2>
  <ul>
    <li><a href="/zh/docs/usage/first-atom">写你的第一个原子</a></li>
    <li><a href="/zh/docs/usage/mcp-claude">接入 Claude Code</a></li>
    <li><a href="/zh/docs/usage/publish-install">发布与安装自己的 Prime</a></li>
  </ul>
</DocsLayout>
